# Examples 1:
# Project with only source/config files - no artifacts
# Expected: Empty output

# Example 2:
# Project with artifacts like pycache, dist, .env.local, .vscode
# Expected: Comprehensive .gitignore with all relevant patterns

# Current case: Only HTML files modified (admin.html, index.html, siswa.html)
# These are source code files, no build artifacts or dependencies detected
# According to rule 6: If change list contains ONLY source/config files and NO build artifacts, output empty

# Since we have only HTML files (source code) and no build artifacts, dependencies, or temp files,
# we output nothing per rule 6.